matrix:
  include:
    - language: java
      jdk: openjdk11

    - language: node_js
      node_js:
        - 14
      cache:
        directories:
          - node_modules
      env:
        CI: false
      script:
        - cd web
        - npm run build
        - npm test